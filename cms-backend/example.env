# Local PostgreSQL connection for development
# Update these values if your Postgres user/password/database differ
DATABASE_URL="postgresql://postgres:postres@localhost:5432/d_fullstack_cms?schema=public"

# JWT secret for signing tokens (override in production)
JWT_SECRET=changeme_dev_secret

# Seed admin credentials (used by prisma/seed.ts)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme123
